import { z } from 'zod';

import {
  createTRPCRouter,
  publicProcedure,
  protectedProcedure,
} from '~/server/api/trpc';

/**
 * 
 */
export const createUserRouter = createTRPCRouter({
  /**
   * Create new user in the db
   * @params 
   * @arguments
   */

})